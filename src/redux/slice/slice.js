import { createSlice } from '@reduxjs/toolkit'

const initialState = {
  selectedProduct: {},
}

export const selectProduct = createSlice({
    name: 'sale',
    initialState,
    reducers: {
        
    }
})